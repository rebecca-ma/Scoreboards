<%= render 'shared/admin_header', title: 'Edit Board' %>
<div id='content'>
  <%= render 'form', submit_msg: 'Save Changes' %>
  <div id='foot_links'>
    <p><%= link_to 'Cancel', admin_board_path(@board) %></p>
  </div>
</div>
